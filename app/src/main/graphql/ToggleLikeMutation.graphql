mutation ToggleLikeMutation(
    $id: Int,
    $type: LikeableType
) {
    ToggleLike(
        id: $id,
        type: $type
    ) {
        id
        name
        avatar {
            medium
        }
    }
}