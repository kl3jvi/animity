query StaffQuery($id: Int) {
    Staff(id: $id) {
        id
        name {
            full
            native
        }
        image {
            large
        }
        isFavourite
        siteUrl
        favourites
    }
}