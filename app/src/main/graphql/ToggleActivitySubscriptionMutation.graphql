mutation ToggleActivitySubsriptionMutation(
    $activityId: Int,
    $subscribe: Boolean
) {
    ToggleActivitySubscription(
        activityId: $activityId,
        subscribe: $subscribe
    ) {
        __typename
    }
}